<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Odyssey AI - Odyssey</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>

        /* --- Universal Styles & Theme Variables --- */
* { margin: 0; padding: 0; box-sizing: border-box; }

:root {
    --bg-gradient-start: #0f172a;
    --bg-gradient-end: #1e293b;
    --card-bg: rgba(255, 255, 255, 0.05);
    --card-border: rgba(255, 255, 255, 0.1);
    --text-primary: #ffffff;
    --text-secondary: #94a3b8;
    --accent-primary: #667eea;
    --accent-secondary: #764ba2;
    --input-bg: rgba(255, 255, 255, 0.05);
}

body.light-theme {
    --bg-gradient-start: #f8fafc;
    --bg-gradient-end: #e0e7ff;
    --card-border: rgba(102, 126, 234, 0.2);
    --text-primary: #1e293b;
    --input-bg: rgba(241, 245, 249, 0.8);
    --card-bg: rgba(255, 255, 255, 0.8);
}

/* --- Base Layout Styles --- */
body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
    min-height: 100vh;
    color: var(--text-primary);
    display: flex;
    flex-direction: column;
}

body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.05) 0%, transparent 40%), radial-gradient(circle at 80% 80%, rgba(118, 75, 162, 0.05) 0%, transparent 40%);
    pointer-events: none;
    z-index: -1;
}

.content-wrapper {
    position: relative;
    max-width: 900px;
    width: 100%;
    margin: 8rem auto 2rem;
    z-index: 1;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

/* --- Chat Container --- */
.chat-container {
    display: flex;
    flex-direction: column;
    height: 100%;
    max-height: 80vh;
    background: var(--card-bg);
    backdrop-filter: blur(30px);
    border: 1px solid var(--card-border);
    border-radius: 24px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    overflow: hidden;
}

.chat-header {
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--card-border);
    text-align: center;
}

.chat-header h1 {
    font-size: 1.25rem;
    font-weight: 600;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0;
}

.chat-window {
    flex-grow: 1;
    padding: 1.5rem;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

/* --- Message Bubbles & Loader --- */
.message {
    display: flex;
    gap: 0.75rem;
    max-width: 80%;
    align-items: flex-end;
}

.message .avatar {
    font-size: 1.5rem;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--input-bg);
    flex-shrink: 0;
}

.message .text {
    padding: 0.8rem 1.2rem;
    border-radius: 18px;
    line-height: 1.6;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.ai-message { align-self: flex-start; }
.ai-message .text { background: var(--input-bg); border-top-left-radius: 4px; }

.user-message { align-self: flex-end; flex-direction: row-reverse; }
.user-message .avatar { background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); }
.user-message .text { background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); color: white; border-top-right-radius: 4px; }

.typing-indicator { display: flex; gap: 5px; align-items: center; justify-content: center; padding: 0.8rem 1.2rem; }
.typing-indicator span {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--text-secondary);
    animation: bounce 1.4s infinite ease-in-out both;
}
.typing-indicator span:nth-of-type(1) { animation-delay: -0.32s; }
.typing-indicator span:nth-of-type(2) { animation-delay: -0.16s; }
@keyframes bounce {
    0%, 80%, 100% { transform: scale(0); }
    40% { transform: scale(1.0); }
}

/* --- Chat Input Form --- */
.chat-input-form {
    display: flex;
    gap: 1rem;
    padding: 1rem;
    border-top: 1px solid var(--card-border);
    align-items: center;
}

#message-input {
    flex-grow: 1;
    background: var(--input-bg);
    border: 2px solid transparent;
    color: var(--text-primary);
    border-radius: 12px;
    padding: 0.8rem 1.2rem;
    font-family: 'Poppins', sans-serif;
    font-size: 1rem;
    transition: all 0.3s ease;
}

#message-input:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
}

.send-btn {
    width: 44px;
    height: 44px;
    flex-shrink: 0;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    color: white;
    border: none;
    border-radius: 50%;
    font-size: 1.2rem;
    cursor: pointer;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.send-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
}
        
        /* Using the same CSS theme you provided */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root { --bg-gradient-start: #0f172a; --bg-gradient-end: #1e293b; --card-bg: rgba(255, 255, 255, 0.05); --card-border: rgba(255, 255, 255, 0.1); --text-primary: #ffffff; --text-secondary: #94a3b8; --accent-primary: #667eea; --accent-secondary: #764ba2; --nav-bg: rgba(15, 23, 42, 0.8); --input-bg: rgba(255, 255, 255, 0.05); --hover-bg: rgba(102, 126, 234, 0.1); }
        body { font-family: 'Poppins', sans-serif; background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%); min-height: 100vh; color: var(--text-primary); display: flex; flex-direction: column; }
        body::before { content: ''; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.1) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(118, 75, 162, 0.1) 0%, transparent 50%); pointer-events: none; z-index: -1; }
        
        .content-wrapper { position: relative; max-width: 900px; width: 100%; margin: 8rem auto 2rem; z-index: 1; flex-grow: 1; display: flex; flex-direction: column; }

        /* Chat Container */
        .chat-container { display: flex; flex-direction: column; height: 100%; max-height: 75vh; background: var(--card-bg); backdrop-filter: blur(20px); border: 1px solid var(--card-border); border-radius: 24px; box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3); }
        .chat-header { padding: 1.5rem; border-bottom: 1px solid var(--card-border); }
        .chat-header h1 { font-size: 1.5rem; background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin: 0; }
        .chat-window { flex-grow: 1; padding: 1.5rem; overflow-y: auto; }
        
        /* Messages */
        .message { display: flex; gap: 1rem; margin-bottom: 1.5rem; max-width: 85%; }
        .message .avatar { font-size: 1.5rem; padding-top: 5px; }
        .message .text { padding: 1rem; border-radius: 12px; line-height: 1.6; }
        
        .ai-message { align-self: flex-start; }
        .ai-message .text { background: var(--input-bg); border-top-left-radius: 0; }
        
        .user-message { align-self: flex-end; flex-direction: row-reverse; }
        .user-message .text { background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); color: white; border-top-right-radius: 0; }

        .typing-indicator { font-style: italic; color: var(--text-secondary); }

        /* Chat Input Form */
        .chat-input-form { display: flex; gap: 1rem; padding: 1.5rem; border-top: 1px solid var(--card-border); }
        #message-input { flex-grow: 1; background: var(--input-bg); border: 2px solid var(--card-border); color: var(--text-primary); border-radius: 12px; padding: 1rem; font-family: 'Poppins', sans-serif; font-size: 1rem; transition: border-color 0.3s; }
        #message-input:focus { outline: none; border-color: var(--accent-primary); }
        .send-btn { background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); color: white; border: none; border-radius: 12px; padding: 0 2rem; font-size: 1.2rem; cursor: pointer; transition: transform 0.2s; }
        .send-btn:hover { transform: scale(1.05); }

        /* Universal Settings & Theme Variables */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --card-border: rgba(255, 255, 255, 0.1);
    --text-primary: #ffffff;
    --accent-primary: #667eea;
    --accent-secondary: #764ba2;
    --nav-bg: rgba(15, 23, 42, 0.8);
    --hover-bg: rgba(102, 126, 234, 0.1);
}

body.light-theme {
    --card-border: rgba(102, 126, 234, 0.2);
    --text-primary: #1e293b;
    --nav-bg: rgba(255, 255, 255, 0.9);
    --hover-bg: rgba(102, 126, 234, 0.08);
}

/* --- Floating Navigation Container --- */
.floating-nav {
    position: fixed;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    width: 90%;
    max-width: 1200px;
    background: var(--nav-bg);
    backdrop-filter: blur(20px);
    border-radius: 20px;
    padding: 1rem 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    border: 1px solid var(--card-border);
    z-index: 1000;
    animation: slideDown 0.6s ease;
}

@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateX(-50%) translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(-50%) translateY(0);
    }
}

/* --- Brand/Logo Styling --- */
.nav-brand a {
    text-decoration: none;
    color: inherit;
}

.nav-brand h2 {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

/* --- Navigation Links --- */
.nav-links {
    display: flex;
    list-style: none;
    gap: 2rem;
}

.nav-links a {
    color: var(--text-primary);
    text-decoration: none;
    font-weight: 500;
    padding: 0.5rem 1.5rem;
    border-radius: 10px;
    transition: all 0.3s ease;
    position: relative;
}

.nav-links a:hover {
    background: var(--hover-bg);
}

.nav-links a::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    transition: width 0.3s ease;
}

.nav-links a:hover::before,
.nav-links a.active::before {
    width: 80%;
}


/* --- Theme Toggle Switch --- */
.theme-switch-wrapper {
    display: flex;
    align-items: center;
}

.theme-switch {
    display: inline-block;
    height: 40px;
    position: relative;
    width: 70px;
}

.theme-switch input {
    display: none;
}

.slider {
    background: linear-gradient(135deg, #1e293b, #334155);
    bottom: 0;
    cursor: pointer;
    left: 0;
    position: absolute;
    right: 0;
    top: 0;
    transition: 0.4s;
    border-radius: 34px;
}

body.light-theme .slider {
    background: linear-gradient(135deg, #fbbf24, #f59e0b);
}

.slider:before {
    background-color: white;
    bottom: 4px;
    content: "";
    height: 32px;
    left: 4px;
    position: absolute;
    transition: 0.4s;
    width: 32px;
    border-radius: 50%;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

input:checked + .slider:before {
    transform: translateX(30px);
}

.slider .icon {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    font-size: 1.2rem;
    transition: opacity 0.3s;
}

.slider .moon {
    left: 8px;
    opacity: 1;
}

.slider .sun {
    right: 8px;
    opacity: 0.3;
}

body.light-theme .slider .moon {
    opacity: 0.3;
}

body.light-theme .slider .sun {
    opacity: 1;
}

/* --- Responsive Design for Nav --- */
@media (max-width: 768px) {
    .floating-nav {
        top: 1rem;
        width: 95%;
        padding: 1rem;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .nav-links {
        gap: 1rem;
        order: 3;
        width: 100%;
        justify-content: center;
    }

    .nav-links a {
        padding: 0.5rem 1rem;
        font-size: 0.9rem;
    }
}

:root {
    --bg-gradient-start: #0f172a;
    --bg-gradient-end: #1e293b;
    --card-bg: rgba(255, 255, 255, 0.05);
    --card-border: rgba(255, 255, 255, 0.1);
    --text-primary: #ffffff;
    --text-secondary: #94a3b8;
    --accent-primary: #667eea;
    --accent-secondary: #764ba2;
    --nav-bg: rgba(15, 23, 42, 0.8);
    --input-bg: rgba(255, 255, 255, 0.05);
    --input-border: rgba(255, 255, 255, 0.1);
    --hover-bg: rgba(102, 126, 234, 0.1);
}

body.light-theme {
    --bg-gradient-start: #f8fafc;
    --bg-gradient-end: #e0e7ff;
    --card-bg: rgba(255, 255, 255, 0.9);
    --card-border: rgba(102, 126, 234, 0.2);
    --text-primary: #1e293b;
    --text-secondary: #64748b;
    --nav-bg: rgba(255, 255, 255, 0.9);
    --input-bg: rgba(248, 250, 252, 0.8);
    --input-border: rgba(102, 126, 234, 0.2);
    --hover-bg: rgba(102, 126, 234, 0.08);
}

/* Theme Toggle */
.theme-switch-wrapper {
    display: flex;
    align-items: center;
}

.theme-switch {
    display: inline-block;
    height: 40px;
    position: relative;
    width: 70px;
}

.theme-switch input {
    display: none;
}

.slider {
    background: linear-gradient(135deg, #1e293b, #334155); /* Dark theme slider background */
    bottom: 0;
    cursor: pointer;
    left: 0;
    position: absolute;
    right: 0;
    top: 0;
    transition: 0.4s;
    border-radius: 34px;
}

/* Light theme slider background override */
body.light-theme .slider {
    background: linear-gradient(135deg, #fbbf24, #f59e0b);
}

.slider:before {
    background-color: white;
    bottom: 4px;
    content: "";
    height: 32px;
    left: 4px;
    position: absolute;
    transition: 0.4s;
    width: 32px;
    border-radius: 50%;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

input:checked + .slider:before {
    transform: translateX(30px);
}

.slider .icon {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    font-size: 1.2rem;
    transition: opacity 0.3s;
}

/* Icon visibility for dark theme */
.slider .moon {
    left: 8px;
    opacity: 1;
}

.slider .sun {
    right: 8px;
    opacity: 0.3;
}

/* Icon visibility overrides for light theme */
body.light-theme .slider .moon {
    opacity: 0.3;
}

body.light-theme .slider .sun {
    opacity: 1;
}


/* --- Universal & Body Styles --- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
    min-height: 100vh;
    color: var(--text-primary);
}

body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(118, 75, 162, 0.1) 0%, transparent 50%);
    pointer-events: none;
    z-index: -1;
}

.content-wrapper {
    position: relative;
    max-width: 900px;
    width: 100%;
    margin: 8rem auto 2rem;
    z-index: 1;
}

/* --- Main Container for All States --- */
.main-container {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: 24px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    overflow: hidden; /* Keeps chat container corners rounded */
}

/* --- Styles for Upload & Payment States --- */
.upload-payment-container {
    padding: 3rem;
    text-align: center;
}

.upload-payment-container h1 {
    font-size: 2rem;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
}

.upload-payment-container p {
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: 2.5rem;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
}

.file-upload-label {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 250px;
    background: var(--input-bg);
    border: 3px dashed var(--card-border);
    border-radius: 16px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.file-upload-label:hover {
    background: var(--hover-bg);
    border-color: var(--accent-primary);
}

.upload-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
}

#file-name-display {
    color: var(--text-secondary);
    margin-top: 1rem;
    font-size: 1rem;
}

.btn {
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-weight: 600;
    color: white;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    border: none;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: 'Poppins', sans-serif;
    text-decoration: none;
    display: inline-block;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(102, 126, 234, 0.5);
}

.error-message {
    margin-top: -1.5rem; /* Pulls it closer to the element above */
    margin-bottom: 1.5rem;
    color: #f87171; /* A light red for better visibility */
    font-weight: 500;
}

/* --- Chat Container & Components --- */
.chat-container {
    display: flex;
    flex-direction: column;
    max-height: 75vh;
}

.chat-header {
    padding: 1.5rem;
    border-bottom: 1px solid var(--card-border);
}

.chat-header h1 {
    font-size: 1.5rem;
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0;
}

.chat-window {
    flex-grow: 1;
    padding: 1.5rem;
    overflow-y: auto;
    min-height: 400px;
}

.message {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
    max-width: 85%;
    align-items: flex-start;
}

.message .avatar {
    font-size: 1.5rem;
    padding-top: 5px;
}

.message .text {
    padding: 1rem;
    border-radius: 12px;
    line-height: 1.6;
}

.ai-message {
    align-self: flex-start;
}

.ai-message .text {
    background: var(--input-bg);
    border-top-left-radius: 0;
}

.user-message {
    align-self: flex-end;
    flex-direction: row-reverse;
}

.user-message .text {
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    color: white;
    border-top-right-radius: 0;
}

.typing-indicator {
    font-style: italic;
    color: var(--text-secondary);
}

.chat-input-form {
    display: flex;
    gap: 1rem;
    padding: 1.5rem;
    border-top: 1px solid var(--card-border);
}

#message-input {
    flex-grow: 1;
    background: var(--input-bg);
    border: 2px solid var(--card-border);
    color: var(--text-primary);
    border-radius: 12px;
    padding: 1rem;
    font-family: 'Poppins', sans-serif;
    font-size: 1rem;
    transition: border-color 0.3s ease;
}

#message-input:focus {
    outline: none;
    border-color: var(--accent-primary);
}

.send-btn {
    background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    color: white;
    border: none;
    border-radius: 12px;
    padding: 0 2rem;
    font-size: 1.2rem;
    cursor: pointer;
    transition: transform 0.2s ease;
}

.send-btn:hover {
    transform: scale(1.05);
}
    </style>
</head>
<body>

    <nav class="floating-nav">
        <div class="nav-brand">
            <a href="/">
                <h2>Odyssey ✨</h2>
            </a>
        </div>
        <ul class="nav-links">
            <li><a href="/squiz">Quizify</a></li>
            <li><a href="/summarizer">Precis</a></li>
        </ul>
        <div class="credits-display" style="display: flex; align-items: center; gap: 1rem;">
            <a href="/credits" style="text-decoration: none;">
                <div style="padding: 0.5rem 1rem; background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); border-radius: 20px; color: white; font-weight: 600; display: flex; align-items: center; gap: 0.5rem; transition: all 0.3s ease; cursor: pointer;">
                    <span>⚡</span>
                    <span><%= typeof credits !== 'undefined' ? credits : '0' %> Credits</span>
                </div>
            </a>
            <div class="theme-switch-wrapper">
            <label class="theme-switch" for="theme-toggle">
                <input type="checkbox" id="theme-toggle" />
                <div class="slider round">
                    <span class="icon moon">🌙</span>
                    <span class="icon sun">☀️</span>
                </div>
            </label>
        </div>
    </nav>

    <div class="content-wrapper">
    <div class="chat-container">
        <div class="chat-header">
            <h1>Chat with Odyssey AI</h1>
        </div>
        <div class="chat-window" id="chat-window">
            <div class="message ai-message">
                <span class="avatar">🤖</span>
                <div class="text">
                    Hello! How can I help you today?
                </div>
            </div>
        </div>
        <form id="chat-form" class="chat-input-form">
            <input type="text" id="message-input" placeholder="Ask a question..." autocomplete="off" required>
            <button type="submit" class="send-btn">➤</button>
        </form>
    </div>
    </div>

    <script>

        // --- Theme Toggle Functionality ---
        const themeToggle = document.getElementById('theme-toggle');
        const body = document.body;
        
        // Check for saved theme preference
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme === 'light') {
            body.classList.add('light-theme');
            themeToggle.checked = true;
        }

        themeToggle.addEventListener('change', () => {
            if (themeToggle.checked) {
                body.classList.add('light-theme');
                localStorage.setItem('theme', 'light');
            } else {
                body.classList.remove('light-theme');
                localStorage.setItem('theme', 'dark');
            }
        });

        const chatForm = document.getElementById('chat-form');
const messageInput = document.getElementById('message-input');
const chatWindow = document.getElementById('chat-window');

// Handles form submission
chatForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const userMessage = messageInput.value.trim();
    if (!userMessage) return;

    appendMessage(userMessage, 'user');
    messageInput.value = '';

    const typingIndicator = appendMessage('', 'ai', true); // Show bouncing loader

    try {
        const response = await fetch('/chat', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ message: userMessage })
        });
        
        typingIndicator.remove(); // Remove loader

        if (!response.ok) {
            const errorData = await response.json();
            throw new Error(errorData.error || 'Something went wrong');
        }
        
        const data = await response.json();
        appendMessage(data.answer, 'ai'); // Display AI response

    } catch (error) {
        typingIndicator.remove();
        appendMessage(`Error: ${error.message}`, 'ai'); // Display error
    }
});

// Function to add new messages to the chat window
function appendMessage(text, role, isTyping = false) {
    const messageDiv = document.createElement('div');
    messageDiv.classList.add('message', role === 'user' ? 'user-message' : 'ai-message');
    
    const avatar = role === 'user' ? '👤' : '🤖';
    let contentHtml;

    if (isTyping) {
        // HTML for the bouncing loader
        contentHtml = `
            <div class="avatar">${avatar}</div>
            <div class="text">
                <div class="typing-indicator">
                    <span></span><span></span><span></span>
                </div>
            </div>`;
    } else {
        // HTML for a regular message
        contentHtml = `
            <div class="avatar">${avatar}</div>
            <div class="text">${text.replace(/\n/g, '<br>')}</div>`;
    }
    
    messageDiv.innerHTML = contentHtml;
    chatWindow.appendChild(messageDiv);
    chatWindow.scrollTop = chatWindow.scrollHeight; // Auto-scroll to the bottom
    return messageDiv;
}
    </script>
</body>
</html>