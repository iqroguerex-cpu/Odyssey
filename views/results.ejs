<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Results</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 1rem;
            line-height: 1.6;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        h1 {
            color: white;
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            animation: fadeInDown 0.6s ease;
        }

        .score-card {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            animation: scaleIn 0.5s ease;
        }

        .score-display {
            font-size: 4rem;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin: 1rem 0;
        }

        .percentage {
            font-size: 1.5rem;
            color: #4a5568;
            margin-bottom: 0.5rem;
        }

        .performance-message {
            font-size: 1.2rem;
            color: #718096;
            margin-top: 0.5rem;
        }

        .results-summary {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            margin-bottom: 2rem;
            animation: fadeInUp 0.6s ease 0.2s both;
        }

        .question-result {
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: #f8f9fa;
            border-radius: 12px;
            border-left: 4px solid #667eea;
        }

        h3 {
            color: #2d3748;
            font-size: 1.2rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        ul {
            list-style: none;
        }

        li {
            padding: 1rem;
            margin: 0.5rem 0;
            background: white;
            border-radius: 8px;
            border: 2px solid #e2e8f0;
            transition: all 0.3s ease;
        }

        .correct {
            border-color: #48bb78;
            background: linear-gradient(135deg, #48bb7810 0%, #38a16920 100%);
            position: relative;
        }

        .correct::before {
            content: "✓";
            position: absolute;
            left: 1rem;
            color: #48bb78;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .correct {
            padding-left: 3rem;
        }

        .incorrect {
            border-color: #f56565;
            background: linear-gradient(135deg, #f5656510 0%, #e5353520 100%);
            position: relative;
        }

        .incorrect::before {
            content: "✗";
            position: absolute;
            left: 1rem;
            color: #f56565;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .incorrect {
            padding-left: 3rem;
        }

        .user-answer {
            border-width: 3px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .answer-label {
            display: inline-block;
            margin-left: 0.5rem;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .correct-label {
            background: #48bb78;
            color: white;
        }

        .your-answer-label {
            background: #f56565;
            color: white;
        }

        hr {
            display: none;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            animation: fadeInUp 0.6s ease 0.4s both;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2.5rem;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 10px;
            transition: all 0.3s ease;
            text-align: center;
        }

        .btn-primary {
            color: white;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
        }

        .btn-secondary {
            color: #667eea;
            background: white;
            border: 2px solid #667eea;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .btn-secondary:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes scaleIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }

            h1 {
                font-size: 2rem;
            }

            .score-card {
                padding: 1.5rem;
            }

            .score-display {
                font-size: 3rem;
            }

            .percentage {
                font-size: 1.2rem;
            }

            .performance-message {
                font-size: 1rem;
            }

            .results-summary {
                padding: 1.5rem;
            }

            .question-result {
                padding: 1rem;
            }

            h3 {
                font-size: 1.1rem;
            }

            li {
                padding: 0.75rem;
                font-size: 0.95rem;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
                padding: 0.875rem 2rem;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Quiz Results</h1>

        <div class="score-card">
            <div class="percentage"><%= percentage %>%</div>
            <div class="score-display"><%= score %>/<%= total %></div>
            <div class="performance-message">
                <% if (percentage >= 90) { %>
                    🎉 Outstanding! You're a quiz master!
                <% } else if (percentage >= 70) { %>
                    👏 Great job! Well done!
                <% } else if (percentage >= 50) { %>
                    👍 Good effort! Keep learning!
                <% } else { %>
                    💪 Don't give up! Practice makes perfect!
                <% } %>
            </div>
        </div>

        <div class="results-summary">
            <% results.forEach((result, index) => { %>
                <div class="question-result">
                    <h3><%= (index + 1) %>. <%= result.question %></h3>
                    <ul>
                        <% result.options.forEach((option, optionIndex) => { %>
                            <% let classes = []; %>
                            <% if (optionIndex === result.correctAnswerIndex) classes.push('correct'); %>
                            <% if (optionIndex === result.userAnswerIndex && !result.isCorrect) classes.push('incorrect'); %>
                            <% if (optionIndex === result.userAnswerIndex) classes.push('user-answer'); %>
                            
                            <li class="<%= classes.join(' ') %>">
                                <%= option %>
                                <% if (optionIndex === result.correctAnswerIndex) { %>
                                    <span class="answer-label correct-label">Correct Answer</span>
                                <% } %>
                                <% if (optionIndex === result.userAnswerIndex && !result.isCorrect) { %>
                                    <span class="answer-label your-answer-label">Your Answer</span>
                                <% } %>
                            </li>
                        <% }); %>
                    </ul>
                </div>
            <% }); %>
        </div>

        <div class="action-buttons">
            <a href="/squiz" class="btn btn-primary">Try Another Quiz</a>
            <a href="/" class="btn btn-secondary">Back to Dashboard</a>
        </div>
    </div>
</body>
</html>